CREATE TABLE hibernate_sequence (
    next_val bigint
);

INSERT INTO hibernate_sequence VALUES(10);

CREATE TABLE verification_token_seq (
    next_val bigint
);
INSERT INTO verification_token_seq VALUES(10);
